/*import {filterData,sortData} from './data.js';
import data from "./data/ghibli/ghibli.js";

describe('Test FilterData Search', () => {
    it('filter title', () => {
        let searchTextValue = document.getElementById("searchMovie").value;
        const arr=filterData(data.films, searchTextValue);
        expect(arr).toEqual();
    });
})*/